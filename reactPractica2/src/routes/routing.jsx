import react from 'react'
import { BrowserRouter as Router, Route, NavLink, Routes } from 'react-router-dom'

const Routing = () =>{
return(
    <Router>
        <Routes>
            <routing></routing>
            <routing></routing>
            <routing></routing>
            <routing></routing>
            <routing></routing>
        </Routes>
    </Router>
)

}